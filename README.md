# JSON Parser
This file converts python code into corresponding JSON format.

After cloning the ropsitory, open the directory and run this command

```
pip install -r requirements.txt
```

To run this code, follow these steps:
1. Make your changes to ref.py
2. Run main.py
3. Fetch the converted json to out.json
